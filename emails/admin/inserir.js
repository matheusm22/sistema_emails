"use strict";

$(function () {
    $("#email").keypress(function (event) {
        if (event.which == 32) {
            return false;
        }
    });

});

$(function () {
    $("#senha").keypress(function (event) {
        if (event.which == 32) {
            return false;
        }
    });

});


$(function () {
    $("#sophia").keypress(function (event) {
        if (event.which == 32) {
            return false;
        }
    });

});  